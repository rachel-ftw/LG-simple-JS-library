const filter = (collection, [predicate=_.identity]) => {
  let result = []
  for (let index = 0; index < collection.length; index++){
    //checks each object entry within the array
    for (i = 0; i < )
      //checks each item in this object for truthy matching to predicate.
      // if match, push this whole object to the return array
    //push outcome to result array
  }
  return result
}

export { filter }